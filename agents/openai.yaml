interface:
  display_name: "视频音频转写"
  short_description: "Resumable local transcription with safe browser handoff"
  default_prompt: "Use $video-audio-transcribe to process this media with resumable setup and create a faithful spoken script without adding or removing transcript content."
